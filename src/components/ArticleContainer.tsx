import React from "react";
import { propTypes } from "../propTypes";

const ArticleContainer = (props: propTypes) => {
  const { post, order_key } = props;

  return <div>ArticleContainer</div>;
};

export default ArticleContainer;
